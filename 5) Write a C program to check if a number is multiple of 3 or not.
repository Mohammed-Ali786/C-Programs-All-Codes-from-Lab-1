#include <stdio.h>
int main()
{
    int n;
    printf("Enter number:");
    scanf("%d", &n);

    if (n%3 ==0)
        printf("Multiple of 3");
    else
        printf("Not Multiple");

    return 0;
}
