 #include <stdio.h>
int main()
{
    int a,b,c;
    printf("Enter 3 angles: ");
    scanf("%d, %d, %d", &a, &b, &c);

    if (a+b+c ==180)
    {
        printf("valid");
    }
    else
    {
        printf("invalid");
    }
    return 0;
}
