#include <stdio.h>
int main()
{
    int a,b;
    printf("Enter the numbers: ");
    scanf("%d, %d", &a, &b);

    if (a<b)
        printf("A is smallest");
    else
        printf("B is smallest");

    return 0;
}
